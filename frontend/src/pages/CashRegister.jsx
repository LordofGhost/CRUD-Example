/** List on the right with all products currently scanned (Product pannel = Name, Price, Count, Delte button)
 *  On the left the product categories as buttons and than the product list when you click on it
 */
import Header from "../components/layout/Header";

function CashRegister() {
  return (
    <div>
      <Header PageName={"Kasse"} />
    </div>
  );
}

export default CashRegister;
